# This Camera is custom I highly recomend using a Deticated Pi 4 camera

# How I made the camera
The bottom Part of the camer the actual Board if you want to see it (it is named camera board) is from a old trail camera that broke a while back. The lens (called lenes for camera) is from a past ai robot with a camera I combiened it and it seems to work but I have no idea if it works with a pi 4. THIS IS NOT A CUSTOM PCB.
